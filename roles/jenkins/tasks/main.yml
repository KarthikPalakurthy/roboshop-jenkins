- name: Create config file
  ansible.builtin.template:
    src: job.xml
    dest: /tmp/job.xml

- name: Create Jobs
  community.general.jenkins_job:
    config: "{{ lookup('file', 'templates/job.xml') }}"
    name: testing
    user: admin
    password: "{{jenkins_password}}"
    url: http://172.31.2.61:8080
  loop:
    - frontend
    - cart
    - catalogue